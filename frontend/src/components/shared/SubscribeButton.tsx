import { Button } from '../ui/button';

const SubscribeButton = ({ children }: { children: React.ReactNode }) => {
  // TODO: Функционал подписок завозить сюда
  return <Button>{children}</Button>;
};

export default SubscribeButton;
